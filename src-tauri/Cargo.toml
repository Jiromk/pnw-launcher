[package]
name = "pnw-launcher"
version = "1.9.4"
edition = "2021"

[dependencies]
# `devtools` reste compilé pour permettre l'usage en dev (cargo run / tauri dev).
# L'ouverture des devtools côté code est gardée derrière `cfg(debug_assertions)`
# dans main.rs → en release elle n'est jamais appelée (anti-cheat client-side).
tauri = { version = "2", features = ["protocol-asset", "devtools"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

# HTTP + TLS + décompression (blocking)
reqwest = { version = "0.12", default-features = false, features = [
  "blocking",
  "rustls-tls",
  "gzip",
  "brotli",
  "deflate",
  "json"
] }

walkdir = "2"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
zip = { version = "0.6", default-features = false, features = ["deflate"] }
dirs = "5"
base64 = "0.22"

# Plugin dialog v2 (pour @tauri-apps/plugin-dialog côté front)
tauri-plugin-dialog = "2"

# Espace disque (vérif avant mise à jour)
sysinfo = "0.31"

# Discord Rich Presence
discord-presence = "3.2"

# Ouvrir URL (navigateur / installateur)
open = "5"
urlencoding = "2"
tauri-plugin-updater = "2"

[build-dependencies]
tauri-build = { version = "2", features = [] }
